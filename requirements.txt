# Core pipeline (required)
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
vaderSentiment>=3.3
matplotlib>=3.7

# Live scoring against a model (optional — only needed to leave offline fixture mode)
# anthropic>=0.40
